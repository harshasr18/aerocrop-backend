services:
  - type: web
    name: aerocrop-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120 --workers 1
    envVars:
      - key: GEE_SERVICE_ACCOUNT_JSON
        sync: false
